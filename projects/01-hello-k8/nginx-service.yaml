apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  # Exposes the service using NodePort.
  # Allows external access via a port on each node.
  type: NodePort
  selector:
    app: nginx-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080